# THIS CODE WAS AUTOGENERATED. DO NOT MODIFY THIS FILE DIRECTLY
# THE SOURCE CODE LIVES IN A DIFFERENT REPOSITORY:
#   - centralized-templates
# FILE STEWARD: @pleo-io/team-devx,@pleo-io/infrastructure-core


# ⚠️ This workflow is empty on purpose until we have some time to craft a replacement solution for synthetic tests or trigger anything else after a successful deployment
name: Successful Deployment Dispatch

on:
  workflow_dispatch:

jobs:
  dummy:
    name: "Dummy job"
    runs-on: ubuntu-latest

    steps:
      - name: Echoes a message
        run: echo "I am a dummy job"
